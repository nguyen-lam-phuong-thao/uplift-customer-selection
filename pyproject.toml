[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "uplift-customer-selection"
version = "0.1.0"
description = "Reusable customer-selection framework with uplift modeling."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]